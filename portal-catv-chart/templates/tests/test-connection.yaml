apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-catv-chart.fullname" . }}-test-connection"
  labels:
    {{- include "portal-catv-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-catv-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
