apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "portal-catv-chart.fullname" . }}-configmap
  labels:
  {{- include "portal-catv-chart.labels" . | nindent 4 }}
data:
{{- toYaml .Values.configMap | nindent 2 }}